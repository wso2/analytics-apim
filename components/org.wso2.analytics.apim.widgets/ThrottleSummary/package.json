{
    "name": "throttle-summary",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "react-vizgrammar": "git://github.com/pahans/react-vizgrammar.git#bump-victory-versions"
    },
    "scripts": {
        "build": "../node_modules/.bin/webpack -p",
        "clean": "rimraf dist",
        "dev": "NODE_ENV=development ../node_modules/.bin/webpack -d --config webpack.config.js --watch --progress"
    },
    "devDependencies": {}
}
