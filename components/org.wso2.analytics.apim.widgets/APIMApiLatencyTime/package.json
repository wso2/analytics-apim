{
    "name": "apim-api-latency-time",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "moment": "^2.24.0",
        "react-vizgrammar": "^0.8.7"
    },
    "scripts": {
        "build": "../node_modules/.bin/webpack -p",
        "clean": "rimraf dist",
        "dev": "NODE_ENV=development ../node_modules/.bin/webpack -d --config webpack.config.js --watch --progress"
    },
    "devDependencies": {}
}
